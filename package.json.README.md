# package.json notes

Human-friendly notes about `package.json` and key dependency groups:

- `dependencies` — runtime libs (Next.js, Prisma client, React, Tailwind)
- `devDependencies` — build/test tooling (TypeScript, ESLint, Vitest)

Use `npm run dev` to start the app in development.
