# components.json — auxiliary components metadata (used by tooling)

This companion file documents the intended purpose and warns to avoid adding inline comments to `components.json` since JSON doesn't allow comments.
