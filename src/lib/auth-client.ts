// Client-side Better Auth wrapper (scaffold)
// Replace pseudo imports with actual Better Auth client SDK imports

// Example (pseudo):
// import { createAuthClient } from '@better-auth/client'
// import { usernameClient } from '@better-auth/client/plugins/username'

// export const authClient = createAuthClient({
//   plugins: [usernameClient()]
// })

export const authClient = null; // placeholder
