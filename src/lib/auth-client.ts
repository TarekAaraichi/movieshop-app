/**
 * Auth client helper (ensured)
 * Thin wrapper around the client-side auth SDK used by client components.
 */

import { createAuthClient } from "better-auth/react"

export const authClient = createAuthClient({});